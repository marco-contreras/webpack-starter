git init                               // Init the project
git commit -m "Init Project"           // Do a commit
git checkout -- .                      // Restore project
git status                             // View status of files